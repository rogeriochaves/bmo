#!/usr/bin/env bash

set -eo pipefail

# if [ "$(uname)" == "Darwin" ]; then
#   brew install portaudio || brew install --build-from-source portaudio
#   pip install PyAudio==0.2.11
# else
#   sudo apt install python3-pyaudio
# fi

# pip install -r requirements.txt